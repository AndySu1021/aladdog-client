import 'package:flutter/material.dart';

class SettleRecord extends StatelessWidget {
  const SettleRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SettleRecord'),
    );
  }
}
