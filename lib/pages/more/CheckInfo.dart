import 'package:flutter/material.dart';

class CheckInfo extends StatelessWidget {
  const CheckInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CheckInfo'),
    );
  }
}
