import 'package:flutter/material.dart';

class DeviceSetting extends StatelessWidget {
  const DeviceSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('DeviceSetting'),
    );
  }
}
