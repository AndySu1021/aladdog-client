import 'package:flutter/material.dart';

class CounterRecord extends StatelessWidget {
  const CounterRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CounterRecord'),
    );
  }
}
