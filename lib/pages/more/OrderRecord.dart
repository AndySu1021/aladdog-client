import 'package:flutter/material.dart';

class OrderRecord extends StatelessWidget {
  const OrderRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('OrderRecord'),
    );
  }
}
