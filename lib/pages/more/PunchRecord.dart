import 'package:flutter/material.dart';

class PunchRecord extends StatelessWidget {
  const PunchRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PunchRecord'),
    );
  }
}
